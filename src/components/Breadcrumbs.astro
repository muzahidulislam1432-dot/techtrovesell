---
const {items=[]}=Astro.props;
---
<nav class="breadcrumbs"><a href="/">Home</a>{items.map((item,i)=><><span>/</span>{i===items.length-1?<span aria-current="page">{item.label}</span>:<a href={item.href}>{item.label}</a>}</>)}</nav>
